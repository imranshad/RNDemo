import { StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection:"row",
      },
      input: {
        borderWidth: 1,
        borderRadius: 20,
        borderColor: "grey",
        width: "100%",
        padding:10,
      }
});
export  { styles}